name: I've had enough of it, enough of it

on:
  push:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    name: Build for ${{ matrix.nickname }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - extra: ""
            nickname: "linux-amd64v4-glibc"
          - extra: "CGO_ENABLED=0"
            nickname: "linux-amd64-musl"
          - extra: "GOARCH=arm64"
            nickname: "linux-arm64-musl"
          - extra: "GOOS=darwin GOARCH=arm64"
            nickname: "darwin-arm64"
          - extra: "GOOS=windows GOARCH=amd64"
            nickname: "windows-amd64"

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Set up Go
        uses: actions/setup-go@main
        with:
          go-version: 'stable'
          
      - name: Build!
        shell: bash
        run: |
          ${{ matrix.extra }} go build
          mv go-rustdesk-server "go-rustdesk-server-${{ matrix.nickname }}"

      - name: Release
        uses: softprops/action-gh-release@master
        with:
          tag_name: V2.0.8
          files: go-rustdesk-server*
          body: |
            # 📝 Release Notes
            - Log reworted
            - I love Claude, It actully is a gineus!
            - Added macOS(arm64), Windows(amd64)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
